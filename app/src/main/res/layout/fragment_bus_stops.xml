<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorSurface">

    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appbar_bus_stops"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <include layout="@layout/view_toolbar"
            android:id="@+id/toolbar_custom"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            app:layout_collapseMode="pin" />
    </com.google.android.material.appbar.AppBarLayout>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <com.kennyc.view.MultiStateView
            android:id="@+id/container_cities"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:animateLayoutChanges="true"
            app:msv_errorView="@layout/view_bus_stops_empty"
            app:msv_emptyView="@layout/view_bus_stops_empty"
            app:msv_loadingView="@layout/view_loading"
            app:msv_viewState="empty">

            <ExpandableListView
                android:id="@+id/list_cities"
                android:layout_height="match_parent"
                android:layout_width="match_parent"
                android:childDivider="@color/colorBackground"
                android:divider="@color/colorBackground"
                android:dividerHeight="1dp" />
        </com.kennyc.view.MultiStateView>
    </FrameLayout>
</androidx.coordinatorlayout.widget.CoordinatorLayout>
